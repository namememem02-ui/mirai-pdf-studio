Read `MIRAI-CONCEPT.md` first — it explains the Mirai ecosystem, team workflow, port registry, and all project conventions.

Project facts: Mee-a-rai PDF Studio — client-side PDF toolbox (16 tools), port 4200, deployed at https://mirai-pdf-studio.vercel.app, registered as a unit in the สำนักจารึก guild of Mee-a-rai Hub.

# Next.js 16 warning
This repo uses Next.js 16 which has breaking changes — read the relevant guide in `node_modules/next/dist/docs/` before writing code. `params` is a Promise (`await params`).
